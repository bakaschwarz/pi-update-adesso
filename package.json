{
  "name": "pi-update-adesso",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint .",
    "postinstall": "npm run build || true"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": ">=1.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  },
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  }
}
